<form method="post" action="{{ path('app_check_delete', {'id': check.id}) }}" onsubmit="return confirm('Вы действительно хотите удалить запись?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ check.id) }}">
    <button class="btn">Удалить</button>
</form>
